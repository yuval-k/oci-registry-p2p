apiVersion: v1
kind: Service
metadata:
  name: {{ include "oci-registry-p2p.fullname" . }}
  labels:
    {{- include "oci-registry-p2p.labels" . | nindent 4 }}
{{- with .Values.service.annotations }}
{{ toYaml . | indent 4 }}
{{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "oci-registry-p2p.selectorLabels" . | nindent 4 }}
