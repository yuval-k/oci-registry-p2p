{{- if .Values.tls.createCertificate }}
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "docker-registry-p2p.fullname" . }}
  namespace: {{ .Release.Namespace }}
spec:
  secretName: {{ include "docker-registry-p2p.fullname" . }}
  issuerRef:
    kind: Issuer # TODO
    name: letsencrypt-staging # TODO
  dnsNames:
  - '*.example.com' # TODO
  - example.com # TODO
  - example.org # TODO
{{- end }}